import { motion } from 'framer-motion';
import { styled } from 'stitches.config';

export const Motion = {
  Picture: styled(motion.picture, {}),
  Div: styled(motion.div, {}),
  Section: styled(motion.section, {}),
};
